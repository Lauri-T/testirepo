var Main = (function($) {
	/*
	 * Handle jQuery ready
	 */
	function handleReady() {
	}
	
	/*
	 * Initialize the universe
	 */
	function init() {
		$(handleReady);
	}
	
	// Reveal public functions
	return {
		init: init
	};
}(jQuery));

